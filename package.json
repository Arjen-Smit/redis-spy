{
  "name": "redis-spy",
  "version": "1.0.0",
  "description": "simple application to read out redis publishes on specific channels.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Arjen Smit",
  "license": "ISC",
  "dependencies": {
    "redis": "^2.8.0"
  }
}
